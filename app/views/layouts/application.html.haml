!!!
%html
  %head
    %title= title
    = render 'layouts/stylesheets'
    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body
    #top
      = render 'layouts/header'
    /ZONE FOR NOTICE AND ALERT MESSAGES
    //////////////////////////////////////////////////////////
    %p.notice= notice
    %p.alert= alert
    /NAVIGATION TABS
    //////////////////////////////////////////////////////////
    - if user_signed_in?
      =render 'layouts/tabs'
    /PAGE CONTENT
    //////////////////////////////////////////////////////////
    #content

      = yield

    #bottom
      = render 'layouts/footer'
      = debug(params) if Rails.env.development?
      = debug(session) if Rails.env.development?
      - if Rails.env.development?
        %nav.user_nav
          %table
            %tr
            %td= link_to "Home", root_path
            - if user_signed_in?
              %td= link_to "Profile", profile_path(current_user.profile.id)
              %td= link_to "Settings", edit_user_registration_path(current_user)
            -else
              %td= link_to "Take the Tour", tour_path
            - if user_signed_in?
              %td= link_to "Sign out", signout_path, :method => :delete
            - else
              %td= link_to "Sign in", signin_path
        /= render 'devise/menu/registration_items'

