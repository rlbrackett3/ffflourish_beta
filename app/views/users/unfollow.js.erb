// store element IDs in variables
var unfollowBtn = $('#<%= escape_javascript("unfollow_me_#{@user.id}") %>');
var followContainer = $('#<%= escape_javascript("follow_link_#{@user.id}") %>');

// render partial to display comment
$(followContainer).empty();
$(followContainer).append("<%= escape_javascript(render 'users/follow') %>").fadeIn(2000);

