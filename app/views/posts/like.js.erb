var voteCount = $('a#<%= escape_javascript("feeditem_votes_count_#{@post.id}") %>');
var voteImage = $('#<%= escape_javascript("feeditem_votes_img_#{@post.id}") %>');

// update comment count
$(voteCount).text("<%= escape_javascript(@post.vote_count.to_s) %>");
// update commented image
var d = new Date();
$(voteImage).attr("src", "/images/like_red.png?"+d.getTime());
