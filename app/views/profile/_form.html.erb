<div id="edit_profile_container">
  <%= form_for(@user.profile, :url => user_profile_path,
                              :html => {:multipart => true}) do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <%= render 'form_base_stats', :f => f %>
    <%= f.submit :class => 'actions' %>
​ <% end %>
</div>
