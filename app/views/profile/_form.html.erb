<%= form_for(@user.profile, :url => user_profile_path) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <%= render 'form_base_stats', :f => f %>
  <%= f.submit :class => 'actions' %>
​<% end %>

